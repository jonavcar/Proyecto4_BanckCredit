server:
  port: 8082

spring:
  profiles:
    active: qas
  application:
    name: banckcredit-microservice

---
spring:
  profiles: dev
  cloud:
    config:
      enabled: true
      uri: http://localhost:9998
---
spring:
  profiles: qas
  cloud:
    config:
      enabled: true
      uri: http://config-server.southcentralus.azurecontainer.io:9998